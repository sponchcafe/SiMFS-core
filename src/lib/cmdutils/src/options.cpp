#include <cmdutils/options.hpp>
#include <cmdutils/logging.hpp>

namespace sim{

    namespace opt{

       
       /* 
        void ParameterHandler::enableConfig(){
        }
        
        void ParameterHandler::enableHelp(std::string helpmessage){
            bool help = false;
                ops_noenv >> GetOpt::OptionPresent('h', "help", help);
                if (help){
                    log::info(helpmessage);
                    log::info(opt_helpmessage);
                    exit(EXIT_SUCCESS);
                }
        }
        */

    };

};
